<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:StudyDocumentManager.Models"
             xmlns:conv="using:StudyDocumentManager.Converters"
             x:Class="StudyDocumentManager.Views.BulkDelete"
             x:DataType="vm:BulkDeleteModel">

    <Border Background="{StaticResource ContentBackground}" Padding="{StaticResource PaddingPage}">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="6" Margin="0,0,0,12">
                <Image Source="{StaticResource IconBulkDelete}" Width="18" Height="18"/>
                <TextBlock Text="{Binding [BulkDelete_Title], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeXl}"
                           FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
            </StackPanel>

            <Border DockPanel.Dock="Top" Background="{StaticResource CardBackground}" CornerRadius="{StaticResource RadiusMd}"
                    BorderBrush="{StaticResource CardBorder}" BorderThickness="1"
                    Padding="{StaticResource PaddingMd}" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="{Binding [Search_Label], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeMd}" VerticalAlignment="Center"
                               Foreground="{StaticResource TextPrimary}"/>
                    <TextBox Text="{Binding SearchKeyword, Mode=TwoWay}" Width="220" FontSize="{StaticResource FontSizeMd}"
                             Height="28"/>
                    <Button Classes="primary" Command="{Binding SearchCommand}" Padding="12,5">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <Image Source="{StaticResource IconSearch}" Width="12" Height="12"/>
                            <TextBlock Text="{Binding [Search_BtnSearch], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}"/>
                        </StackPanel>
                    </Button>

                    <Border Width="1" Height="20" Background="{StaticResource CardBorder}" Margin="4,0"/>

                    <TextBlock Text="{Binding [AddEdit_LblSubject], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox Name="cboSubject" Width="130" FontSize="{StaticResource FontSizeSm}" Height="28" VerticalAlignment="Center"/>

                    <TextBlock Text="{Binding [AddEdit_LblType], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondary}"/>
                    <ComboBox Name="cboType" Width="110" FontSize="{StaticResource FontSizeSm}" Height="28" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border DockPanel.Dock="Bottom" Background="{StaticResource SidebarBackground}"
                    CornerRadius="{StaticResource RadiusMd}" Padding="{StaticResource PaddingMd}" Margin="0,12,0,0">
                <Grid ColumnDefinitions="*,Auto">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Classes="secondary" Command="{Binding SelectAllCommand}" Padding="{StaticResource PaddingBtnMd}">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Image Source="{StaticResource IconCheck}" Width="11" Height="11"/>
                                <TextBlock Text="{Binding [Bulk_BtnSelectAll], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="secondary" Command="{Binding DeselectAllCommand}" Padding="{StaticResource PaddingBtnMd}">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Image Source="{StaticResource IconClose}" Width="11" Height="11"/>
                                <TextBlock Text="{Binding [Bulk_BtnDeselectAll], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}"/>
                            </StackPanel>
                        </Button>

                        <Border Width="1" Height="22" Background="{StaticResource CardBorder}" Margin="4,0"/>

                        <Button Padding="{StaticResource PaddingBtnMd}" Command="{Binding MarkImportantCommand}"
                                Classes="warning">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Image Source="{StaticResource IconStar}" Width="12" Height="12"/>
                                <TextBlock Text="{Binding [BulkDelete_BtnMarkImportant], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <ComboBox Name="cboNewSubject" Width="130" FontSize="{StaticResource FontSizeSm}" Height="28" VerticalAlignment="Center"/>
                        <Button Padding="{StaticResource PaddingBtnMd}" Command="{Binding ChangeSubjectCommand}"
                                Classes="primary">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Image Source="{StaticResource IconCategory}" Width="12" Height="12"/>
                                <TextBlock Text="{Binding [BulkDelete_BtnChangeSubject], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeSm}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                        <Button Padding="{StaticResource PaddingBtnPrimary}" Command="{Binding DeleteSelectedCommand}"
                                Classes="danger">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Image Source="{StaticResource IconDelete}" Width="13" Height="13"/>
                                <TextBlock Text="{Binding [BulkDelete_BtnDelete], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeMd}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                        <Button Classes="secondary" Command="{Binding CancelCommand}" Padding="{StaticResource PaddingMd}">
                            <TextBlock Text="{Binding [Toolbar_GoBack], Source={StaticResource Loc}}" FontSize="{StaticResource FontSizeMd}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="{StaticResource CardBackground}" CornerRadius="{StaticResource RadiusMd}"
                    BorderBrush="{StaticResource CardBorder}" BorderThickness="1" ClipToBounds="True">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusText}" FontSize="{StaticResource FontSizeXs}"
                               Foreground="{StaticResource TextMuted}" Margin="8,4"/>
                    <DataGrid Name="dgDocuments"
                              AutoGenerateColumns="False"
                              CanUserSortColumns="True"
                              GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="✓" Binding="{Binding IsSelected}" Width="40"/>
                            <DataGridTemplateColumn Header="{Binding [AddEdit_LblName], Source={StaticResource Loc}}" Width="*" MinWidth="200" SortMemberPath="Document.Name">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate x:DataType="vm:SelectableDocument">
                                        <StackPanel Orientation="Horizontal" Spacing="8" Margin="8,0">
                                            <Image Source="{Binding Document.Type, Converter={x:Static conv:DocumentTypeIconConverter.Instance}}" 
                                                   Width="16" Height="16" VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding Document.Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="{Binding [AddEdit_LblSubject], Source={StaticResource Loc}}" Binding="{Binding Document.Subject, Mode=OneWay}" Width="120"/>
                            <DataGridTextColumn Header="{Binding [AddEdit_LblType], Source={StaticResource Loc}}" Binding="{Binding Document.Type, Mode=OneWay}" Width="80"/>
                            <DataGridTextColumn Header="{Binding [BulkDelete_ColDateAdded], Source={StaticResource Loc}}" Binding="{Binding Document.CreatedAt, Mode=OneWay, StringFormat=\{0:dd/MM/yyyy\}}" Width="100"/>
                            <DataGridCheckBoxColumn Header="★" Binding="{Binding Document.IsImportant, Mode=OneWay}" Width="40" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </DockPanel>
    </Border>

</UserControl>
