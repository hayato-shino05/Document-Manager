<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:StudyDocumentManager.Models"
        xmlns:loc="using:StudyDocumentManager.Markup"
                     x:Class="StudyDocumentManager.Views.CollectionManagement"
             x:DataType="vm:CollectionManagementModel"
             AutomationProperties.AutomationId="Screen_CollectionManagement">

    <Border Background="{StaticResource ContentBackground}" Padding="{StaticResource PaddingPage}">
        <DockPanel>
            <DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Image Source="{StaticResource IconCollection}" Width="20" Height="20"/>
                    <TextBlock AutomationProperties.AutomationId="Title_CollectionManagement"
                               Text="{loc:Localize CollectionMgmt_Title}" FontSize="{StaticResource FontSizeXl}"
                               FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="6" HorizontalAlignment="Right">
                    <Button Classes="primary" AutomationProperties.AutomationId="CollectionManagement_Create"
                            AutomationProperties.Name="{loc:Localize CollectionMgmt_BtnCreate}"
                            Command="{Binding CreateCollectionCommand}">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <Image Source="{StaticResource IconAddWhite}" Width="16" Height="16"/>
                            <TextBlock Text="{loc:Localize CollectionMgmt_BtnCreate}" FontSize="{StaticResource FontSizeSm}" Foreground="White"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>

            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Grid ColumnDefinitions="300,16,*">

                <Border Background="{StaticResource CardBackground}" CornerRadius="{StaticResource RadiusMd}"
                        BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Background="{StaticResource SidebarBackground}"
                                CornerRadius="6,6,0,0" Padding="{StaticResource PaddingMd}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Image Source="{StaticResource IconCollection}" Width="14" Height="14"/>
                                <TextBlock Text="{loc:Localize CollectionMgmt_ListHeader}" FontSize="{StaticResource FontSizeLg}"
                                           FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                        </Border>

                        <ListBox ItemsSource="{Binding Collections}"
                                 SelectedItem="{Binding SelectedCollection}"
                                 Padding="{StaticResource PaddingIcon}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="6,6">
                                        <Image Source="{StaticResource IconCollection}" Width="14" Height="14"/>
                                        <StackPanel Spacing="2">
                                            <TextBlock Text="{Binding Name}" FontSize="{StaticResource FontSizeMd}"
                                                       FontWeight="Medium" Foreground="{StaticResource TextPrimary}"/>
                                            <TextBlock Text="{Binding Description}" FontSize="{StaticResource FontSizeXs}"
                                                       Foreground="{StaticResource TextSecondary}" TextTrimming="CharacterEllipsis"
                                                       MaxWidth="220"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </Border>

                <Border Grid.Column="2" Background="{StaticResource CardBackground}" CornerRadius="{StaticResource RadiusMd}"
                        BorderBrush="{StaticResource CardBorder}" BorderThickness="1">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Background="{StaticResource SidebarBackground}"
                                CornerRadius="6,6,0,0" Padding="{StaticResource PaddingMd}">
                            <DockPanel>
                                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                    <Image Source="{StaticResource IconDocPage}" Width="14" Height="14"/>
                                    <TextBlock Text="{loc:Localize CollectionMgmt_DetailHeader}" FontSize="{StaticResource FontSizeLg}"
                                               FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"/>
                                </StackPanel>
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" Spacing="4"
                                            HorizontalAlignment="Right">
                                    <Button Classes="secondary" Command="{Binding AddDocumentToCollectionCommand}" Padding="{StaticResource PaddingSm}"
                                            IsEnabled="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}">
                                        <StackPanel Orientation="Horizontal" Spacing="3">
                                            <Image Source="{StaticResource IconAdd}" Width="11" Height="11"/>
                                            <TextBlock Text="{loc:Localize CollectionMgmt_BtnAddDoc}" FontSize="{StaticResource FontSizeSm}"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Classes="secondary" Command="{Binding RenameCollectionCommand}" Padding="{StaticResource PaddingSm}"
                                            IsEnabled="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}">
                                        <StackPanel Orientation="Horizontal" Spacing="3">
                                            <Image Source="{StaticResource IconEdit}" Width="11" Height="11"/>
                                            <TextBlock Text="{loc:Localize CollectionMgmt_BtnRename}" FontSize="{StaticResource FontSizeSm}"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Classes="secondary" Click="OnSelectAllDocumentsClicked"
                                            AutomationProperties.AutomationId="CollectionManagement_SelectAllDocuments"
                                            AutomationProperties.Name="{loc:Localize Bulk_BtnSelectAll}"
                                            IsEnabled="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}"
                                            Padding="{StaticResource PaddingSm}">
                                        <TextBlock Text="{loc:Localize Bulk_BtnSelectAll}" FontSize="{StaticResource FontSizeSm}"/>
                                    </Button>
                                    <Button Classes="secondary" Click="OnDeselectAllDocumentsClicked"
                                            AutomationProperties.AutomationId="CollectionManagement_DeselectAllDocuments"
                                            AutomationProperties.Name="{loc:Localize Bulk_BtnDeselectAll}"
                                            IsEnabled="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}"
                                            Padding="{StaticResource PaddingSm}">
                                        <TextBlock Text="{loc:Localize Bulk_BtnDeselectAll}" FontSize="{StaticResource FontSizeSm}"/>
                                    </Button>
                                    <Button Classes="danger" Command="{Binding RemoveSelectedDocumentsCommand}" Padding="{StaticResource PaddingSm}"
                                            IsEnabled="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}">
                                        <StackPanel Orientation="Horizontal" Spacing="3">
                                            <Image Source="{StaticResource IconDeleteWhite}" Width="11" Height="11"/>
                                            <TextBlock Text="{loc:Localize CollectionMgmt_BtnRemoveDoc}" FontSize="{StaticResource FontSizeSm}"/>
                                        </StackPanel>
                                    </Button>
                                    <Button Classes="danger" Command="{Binding DeleteCollectionCommand}" Padding="{StaticResource PaddingSm}"
                                            IsEnabled="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}">
                                        <StackPanel Orientation="Horizontal" Spacing="3">
                                            <Image Source="{StaticResource IconDeleteWhite}" Width="11" Height="11"/>
                                            <TextBlock Text="{loc:Localize CollectionMgmt_BtnDelete}" FontSize="{StaticResource FontSizeSm}"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </DockPanel>
                        </Border>

                        <Panel>
                            <StackPanel IsVisible="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNull}}"
                                        VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8">
                                <Image Source="{StaticResource IconCollection}" Width="36" Height="36" Opacity="0.4"/>
                                <TextBlock AutomationProperties.AutomationId="CollectionManagement_EmptyState"
                                           AutomationProperties.LiveSetting="Polite"
                                           Text="{loc:Localize CollectionMgmt_EmptyState}"
                                           FontSize="{StaticResource FontSizeMd}" Foreground="{StaticResource TextMuted}"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <DockPanel IsVisible="{Binding SelectedCollection, Converter={x:Static ObjectConverters.IsNotNull}}">
                                <Border DockPanel.Dock="Top" Padding="{StaticResource PaddingMd}">
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="{Binding SelectedCollection.Name}" FontSize="{StaticResource FontSizeLg}"
                                                   FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                                        <TextBlock Text="{Binding SelectedCollection.Description}" FontSize="{StaticResource FontSizeSm}"
                                                   Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <ListBox x:Name="DocumentGrid"
                                         AutomationProperties.AutomationId="CollectionManagement_DocumentGrid"
                                         AutomationProperties.Name="{loc:Localize CollectionMgmt_DetailHeader}"
                                         ItemsSource="{Binding DocumentsInCollection}" SelectionMode="Multiple" Padding="{StaticResource PaddingIcon}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="6,4">
                                                <Image Source="{StaticResource IconDocPage}" Width="16" Height="16"/>
                                                <TextBlock Text="{Binding Name}" FontSize="{StaticResource FontSizeMd}"
                                                           Foreground="{StaticResource TextPrimary}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </DockPanel>
                        </Panel>
                    </DockPanel>
                </Border>
                </Grid>
            </ScrollViewer>
        </DockPanel>
    </Border>

</UserControl>
