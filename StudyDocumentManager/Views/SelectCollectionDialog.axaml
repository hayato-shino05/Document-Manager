<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="using:StudyDocumentManager.Markup"
                x:Class="StudyDocumentManager.Views.SelectCollectionDialog"
        AutomationProperties.AutomationId="Dialog_SelectCollection"
        Title="{loc:Localize SelectCollDialog_Title}"
        Width="440" MaxHeight="520" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        CanResize="True"
        ShowInTaskbar="False"
        Background="{StaticResource ContentBackground}">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" Spacing="12">

        <!-- Document name label -->
        <TextBlock x:Name="DocNameLabel"
                   FontSize="{StaticResource FontSizeLg}" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimary}"
                   TextWrapping="Wrap"/>

        <!-- Collection chips -->
        <StackPanel>
            <TextBlock Text="{loc:Localize SelectCollDialog_SelectChips}"
                       FontSize="{StaticResource FontSizeXs}" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>

            <!-- Empty state -->
            <TextBlock x:Name="EmptyStateText"
                       AutomationProperties.AutomationId="SelectCollection_EmptyState"
                       AutomationProperties.LiveSetting="Polite"
                       Text="{loc:Localize SelectCollDialog_EmptyState}"
                       FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource TextMuted}"
                       IsVisible="False" TextWrapping="Wrap"/>

            <!-- Chip list -->
            <ItemsControl x:Name="ChipsPanel">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Classes="chip"
                                AutomationProperties.Name="{Binding Label}"
                                Margin="0,0,6,6"
                                Tag="{Binding Id}"
                                Click="OnChipClicked">
                            <TextBlock Text="{Binding Label}"/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Selected indicator -->
        <TextBlock x:Name="SelectedLabel"
                   AutomationProperties.AutomationId="SelectCollection_Selected"
                   AutomationProperties.LiveSetting="Polite"
                   FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource TextSecondary}"
                   Text="{loc:Localize SelectCollDialog_NoneSelected}"/>

        <!-- Buttons: OK first, then Cancel -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8" Margin="0,8,0,0">
            <Button x:Name="OkButton"
                    AutomationProperties.AutomationId="SelectCollection_Confirm"
                    AutomationProperties.Name="{loc:Localize SelectCollDialog_BtnAdd}"
                    Content="{loc:Localize SelectCollDialog_BtnAdd}"
                    Classes="primary"
                    IsDefault="True"
                    IsEnabled="False"
                    MinWidth="100"
                    HorizontalContentAlignment="Center"/>
            <Button x:Name="CancelButton"
                    AutomationProperties.AutomationId="SelectCollection_Cancel"
                    AutomationProperties.Name="{loc:Localize Action_Cancel}"
                    Content="{loc:Localize Action_Cancel}"
                    Classes="secondary"
                    IsCancel="True"
                    MinWidth="80"
                    HorizontalContentAlignment="Center"/>
        </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
