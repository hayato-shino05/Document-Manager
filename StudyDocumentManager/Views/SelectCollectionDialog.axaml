<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="StudyDocumentManager.Views.SelectCollectionDialog"
        Title="{Binding [SelectCollDialog_Title], Source={StaticResource Loc}}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False"
        Background="{StaticResource ContentBackground}">

    <StackPanel Margin="24" Spacing="12">

        <!-- Document name label -->
        <TextBlock x:Name="DocNameLabel"
                   FontSize="{StaticResource FontSizeLg}" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimary}"
                   TextWrapping="Wrap"/>

        <!-- Collection chips -->
        <StackPanel>
            <TextBlock Text="{Binding [SelectCollDialog_SelectChips], Source={StaticResource Loc}}"
                       FontSize="{StaticResource FontSizeXs}" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>

            <!-- Empty state -->
            <TextBlock x:Name="EmptyStateText"
                       Text="{Binding [SelectCollDialog_EmptyState], Source={StaticResource Loc}}"
                       FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource TextMuted}"
                       IsVisible="False" TextWrapping="Wrap"/>

            <!-- Chip list -->
            <ItemsControl x:Name="ChipsPanel">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Classes="chip"
                                Margin="0,0,6,6"
                                Tag="{Binding Id}"
                                Click="OnChipClicked">
                            <TextBlock Text="{Binding Label}"/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Selected indicator -->
        <TextBlock x:Name="SelectedLabel"
                   FontSize="{StaticResource FontSizeSm}" Foreground="{StaticResource TextSecondary}"
                   Text="{Binding [SelectCollDialog_NoneSelected], Source={StaticResource Loc}}"/>

        <!-- Buttons: OK first, then Cancel -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8" Margin="0,8,0,0">
            <Button x:Name="OkButton"
                    Content="{Binding [SelectCollDialog_BtnAdd], Source={StaticResource Loc}}"
                    Classes="primary"
                    IsDefault="True"
                    IsEnabled="False"
                    MinWidth="100"
                    HorizontalContentAlignment="Center"/>
            <Button x:Name="CancelButton"
                    Content="{Binding [Action_Cancel], Source={StaticResource Loc}}"
                    Classes="secondary"
                    IsCancel="True"
                    MinWidth="80"
                    HorizontalContentAlignment="Center"/>
        </StackPanel>
    </StackPanel>
</Window>
